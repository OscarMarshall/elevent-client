(ns util
  (:require
    [cljs-time.format :refer [formatter]]
    [page]))

(def date-formatter (formatter page/date-format))
(def time-formatter (formatter page/time-format))
(def datetime-formatter (formatter page/datetime-format))

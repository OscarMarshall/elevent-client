(ns util
  (:require
    [cljs-time.format :refer [formatter]]

    [app]))

(def date-formatter (formatter app/date-format))
(def time-formatter (formatter app/time-format))
(def datetime-formatter (formatter app/datetime-format))
(def input-date-time-formatter (formatter app/input-date-time-format))
